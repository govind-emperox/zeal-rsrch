# Database Schema

Database migrations and local Supabase configuration belong in this directory. The Release 1 schema is defined in the `projects`, `tasks`, `messages`, `run_events`, `sources`, `reports`, `files`, and `cleanup_audits` sections of the PRD.

Implement migrations after the Codex integration spike establishes the task lifecycle and event payload contracts.
